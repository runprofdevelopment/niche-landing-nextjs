/**
 * Firebase App initialization and configuration.
 */

export { getFirebaseClientConfig, type FirebaseClientConfig } from "./config";
export { getFirebaseApp, resetFirebaseAppCache } from "./initialize";
