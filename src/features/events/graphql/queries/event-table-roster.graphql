query EventTableRoster($tableId: ID!) {
  eventTableRoster(tableId: $tableId) {
    seats {
      id
      seatIndex
      guest {
        id
        name
      }
      guestId
      hallId
    }
  }
}
