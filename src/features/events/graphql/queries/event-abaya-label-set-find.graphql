query EventAbayaLabelSetFind($eventId: ID!) {
  eventAbayaLabelSetFind(eventId: $eventId) {
    assignedLabels
    availableLabels
    createdAt
    createdBy
    eventId
    from
    id
    prefix
    suffix
    to
    totalLabels
    updatedAt
    updatedBy
  }
}
