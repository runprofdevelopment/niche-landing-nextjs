query EventGuests($eventId: ID!) {
  eventGuests(eventId: $eventId) {
    id
    eventId
    name
    familyName
    email
    phone
    gender
    abayaLabel
    code
    rsvpStatus
    groupId
    hallId
    seatId
    tableId
    checkedInAt
    checkedOutAt
  }
}
